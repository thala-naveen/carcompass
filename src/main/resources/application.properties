spring.application.name=carcompass

# MongoDB configuration
# The application will use the MONGO_URL environment variable provided by Railway.
# If the environment variable is not set, it will fall back to the local MongoDB instance.
spring.data.mongodb.uri=${MONGO_URL:mongodb://localhost:27017/carcompass}
